# AI Network Traffic Analyzer Configuration

# Database Configuration
# Supported types: 'sqlite' or 'postgres'
# DB_TYPE=sqlite

# For SQLite (default if not set):
DATABASE_URL=sqlite:///network_analyzer.db

# For PostgreSQL (e.g. Supabase, Neon, Render, Railway, Local):
# DATABASE_URL=postgresql://username:password@localhost:5432/network_analyzer
